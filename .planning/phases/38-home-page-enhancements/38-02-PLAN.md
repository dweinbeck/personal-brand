---
phase: 38-home-page-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tutorials.ts
  - src/components/building-blocks/TutorialCard.tsx
  - src/app/page.tsx
  - src/components/home/ToolsShowcase.tsx
autonomous: true

must_haves:
  truths:
    - "Building block cards show reading time (e.g. 5 min read) below description and above tags"
    - "Reading time appears on both home page and /building-blocks listing"
    - "A new Explore Development Tools section appears after the Apps grid on the home page"
    - "Dev Tools section displays tool cards from getTools() data source"
  artifacts:
    - path: "src/lib/tutorials.ts"
      provides: "Reading time calculation based on word count"
      contains: "readingTime"
    - path: "src/components/building-blocks/TutorialCard.tsx"
      provides: "Reading time display below description"
      contains: "min read"
    - path: "src/components/home/ToolsShowcase.tsx"
      provides: "Dev Tools section with tool cards and View All link"
      contains: "getTools"
    - path: "src/app/page.tsx"
      provides: "ToolsShowcase rendered after AppsGrid"
      contains: "ToolsShowcase"
  key_links:
    - from: "src/components/building-blocks/TutorialCard.tsx"
      to: "src/lib/tutorials.ts"
      via: "Tutorial.metadata.readingTime field"
      pattern: "readingTime"
    - from: "src/components/home/ToolsShowcase.tsx"
      to: "src/data/tools.ts"
      via: "getTools() import"
      pattern: "getTools"
    - from: "src/app/page.tsx"
      to: "src/components/home/ToolsShowcase.tsx"
      via: "import and render"
      pattern: "ToolsShowcase"
---

<objective>
Add reading time to building block cards and create a new Dev Tools showcase section on the home page.

Purpose: Give visitors quick content length signals on tutorials and expose development tools directly from the home page for better discoverability.
Output: Reading time on all tutorial cards, new ToolsShowcase component on home page.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/tutorials.ts
@src/components/building-blocks/TutorialCard.tsx
@src/app/page.tsx
@src/data/tools.ts
@src/app/tools/page.tsx
@src/components/home/AppsGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reading time calculation to tutorials and display on cards</name>
  <files>
    src/lib/tutorials.ts
    src/components/building-blocks/TutorialCard.tsx
  </files>
  <action>
    **src/lib/tutorials.ts changes:**

    1. Add `readingTime: string` to the `TutorialMeta` interface.

    2. In the `getAllTutorials()` function, after loading metadata for each tutorial, calculate reading time from the MDX file's word count. Read the file content with `fs.readFileSync`, strip the metadata export block (everything before the first line after the closing `}`), count words using `content.split(/\s+/).length`, divide by 200 (average reading speed), round up with `Math.ceil`, and produce a string like `"5 min read"`.

    3. Attach the calculated `readingTime` to each tutorial's metadata object. Since metadata comes from the MDX export, override it: `metadata.readingTime = readingTimeString` after extracting. Alternatively, store it alongside: the `Tutorial` interface already has `metadata: TutorialMeta`, so adding `readingTime` to `TutorialMeta` and setting it after extraction is cleanest.

    Implementation detail: After the metadata is loaded (either via import or fallback extraction), read the raw file content, strip the `export const metadata = { ... };` block using a regex like `/export\s+const\s+metadata\s*=\s*\{[\s\S]*?\n\};?\s*/`, count remaining words, compute `Math.ceil(wordCount / 200)`, set `metadata.readingTime = \`${minutes} min read\``.

    **src/components/building-blocks/TutorialCard.tsx changes:**

    1. After the description paragraph (`<p className="mt-3 flex-1 ...">`) and BEFORE the additional tags section, add the reading time display:

    ```tsx
    {tutorial.metadata.readingTime && (
      <p className="mt-2 text-xs text-text-tertiary">
        {tutorial.metadata.readingTime}
      </p>
    )}
    ```

    This positions reading time below description, above tags, as specified in HOME-04.
  </action>
  <verify>
    Run `npm run build` to confirm no TypeScript errors. Check that all 3 MDX tutorials get a valid reading time string (each file has 485-1291 words, so expect 3-7 min read values).
  </verify>
  <done>
    Every TutorialCard displays a reading time like "5 min read" below the description and above the additional tags. This works on both the home page BuildingBlocksCta and the /building-blocks listing page because they share TutorialCard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dev Tools showcase section on home page</name>
  <files>
    src/components/home/ToolsShowcase.tsx
    src/app/page.tsx
  </files>
  <action>
    **Create src/components/home/ToolsShowcase.tsx:**

    Create a new server component that mirrors the pattern of AppsGrid and BuildingBlocksCta. Use the same section structure, heading style, and grid layout.

    1. Import `getTools` from `@/data/tools`, `Card` from `@/components/ui/Card`, `CardButtonLabel` from `@/components/ui/CardButtonLabel`, and `Button` from `@/components/ui/Button`.

    2. Section heading: `"Explore Development Tools"` with the same `text-2xl font-semibold tracking-tight text-text-primary text-center` heading style used by AppsGrid and BuildingBlocksCta.

    3. Subtitle: `"Single-function dev utilities and AI-powered tools"` using `text-text-secondary text-center mt-2 mb-10`.

    4. Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` — same as other home sections.

    5. For each tool, render a card matching the exact pattern from `src/app/tools/page.tsx` (gold topic badge, title, subtitle, description, CardButtonLabel). Copy that card JSX directly. The button label should be `"Open Tool"` for external tools and `"Enter App"` for internal ones (same logic as tools page).

    6. After the grid, add a "View all tools" button linking to `/tools`, matching the BuildingBlocksCta pattern:
    ```tsx
    <div className="mt-8 text-center">
      <Button variant="secondary" size="sm" href="/tools">
        View all tools
      </Button>
    </div>
    ```

    Define `const tagColor = "bg-gold-light text-gold-hover border-gold";` at the top of the file (same as tools page and AppCard).

    **src/app/page.tsx changes:**

    1. Import `ToolsShowcase` from `@/components/home/ToolsShowcase`.
    2. Render `<ToolsShowcase />` between `<AppsGrid />` and `<BuildingBlocksCta />` in the HomePage return JSX.

    The final render order should be: HeroSection, AppsGrid, ToolsShowcase, BuildingBlocksCta.
  </action>
  <verify>
    Run `npm run lint && npm run build` to confirm zero errors. Verify ToolsShowcase.tsx exists and exports a function component. Verify page.tsx imports and renders ToolsShowcase between AppsGrid and BuildingBlocksCta.
  </verify>
  <done>
    Home page displays "Explore Development Tools" section after the Apps grid, showing all 5 tools (New Phase Planner, FRD Interviewer, FRD Generator, Research Assistant, Digital Envelopes) in a card grid with a "View all tools" button linking to /tools. Build and lint pass.
  </done>
</task>

</tasks>

<verification>
- `npm run lint` passes with zero errors
- `npm run build` succeeds
- TutorialCard shows reading time below description and above tags
- Reading time appears on home page and /building-blocks listing
- ToolsShowcase section renders after AppsGrid on home page
- Tool cards display correctly with topic badges and descriptions
- "View all tools" button links to /tools
</verification>

<success_criteria>
1. Building block cards show reading time (e.g. "5 min read") below description and above tags on both home and /building-blocks
2. New "Explore Development Tools" section appears after Apps grid on home page
3. Dev Tools section shows all 5 tools with correct card styling matching the /tools page
4. "View all tools" button links to /tools
5. Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/38-home-page-enhancements/38-02-SUMMARY.md`
</output>
