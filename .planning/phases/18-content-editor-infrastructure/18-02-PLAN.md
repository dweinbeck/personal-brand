---
phase: 18-content-editor-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/control-center/content/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /control-center/content shows a table of all existing Building Blocks tutorials"
    - "Each tutorial row displays title, slug, date, and tags"
    - "Tutorials are sorted by date descending (newest first)"
  artifacts:
    - path: "src/app/control-center/content/page.tsx"
      provides: "Server component rendering tutorial list from getAllTutorials()"
      min_lines: 20
  key_links:
    - from: "src/app/control-center/content/page.tsx"
      to: "src/lib/tutorials.ts"
      via: "import getAllTutorials for data"
      pattern: "import.*getAllTutorials.*from.*tutorials"
---

<objective>
Replace the placeholder content editor page with a real tutorial listing that shows all existing Building Blocks tutorials with their title, slug, date, and tags.

Purpose: This is Success Criterion #1 -- the content page must show existing content before an editor can be built on top of it (Phase 19).
Output: A server component that calls getAllTutorials() and renders a styled table.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key source files to reference:
@src/lib/tutorials.ts (getAllTutorials, Tutorial, TutorialMeta types)
@src/app/control-center/content/page.tsx (current placeholder -- will be replaced)
@src/app/control-center/layout.tsx (wrapping layout with AdminGuard + nav)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace content page placeholder with tutorial list</name>
  <files>src/app/control-center/content/page.tsx</files>
  <action>
Replace the entire content of `src/app/control-center/content/page.tsx` with a server component that lists tutorials.

This is a Server Component (NO "use client" directive). It calls `getAllTutorials()` at the top level and renders the results.

Implementation:

1. Import `getAllTutorials` from `@/lib/tutorials`

2. Export default async function `ContentPage()`

3. Call `const tutorials = await getAllTutorials();`

4. Render a page with:
   - `<h1>` heading: "Building Blocks" (text-2xl font-bold text-gray-900 mb-6)
   - A brief description paragraph: "Manage your Building Blocks tutorials. {tutorials.length} tutorial(s) published." (text-sm text-gray-500 mb-8)
   - If `tutorials.length === 0`: show an empty state message "No tutorials found." in a gray box
   - If tutorials exist: a table with columns Title, Slug, Date, Tags

5. Table styling (use Tailwind):
   - Wrapper: `<div className="overflow-x-auto rounded-lg border border-gray-200">`
   - Table: `<table className="min-w-full divide-y divide-gray-200">`
   - `<thead className="bg-gray-50">` with `<th>` elements: `px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider`
   - `<tbody className="divide-y divide-gray-200 bg-white">`
   - Each `<tr>` has `hover:bg-gray-50` for interactive feel
   - Title cell: `font-medium text-gray-900`
   - Slug cell: `text-sm text-gray-500 font-mono` (monospace to indicate it's a URL slug)
   - Date cell: `text-sm text-gray-500` -- format as-is (YYYY-MM-DD)
   - Tags cell: render each tag as a `<span>` with `inline-flex items-center rounded-full bg-blue-50 px-2 py-0.5 text-xs font-medium text-blue-700 mr-1`

6. Container wrapping: `<div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-12">` (matching existing placeholder pattern)

Do NOT add any client-side interactivity, links to edit pages, or "New Tutorial" buttons -- that comes in Phase 19.
  </action>
  <verify>Run `npm run build` -- must compile. Run `npm run lint` -- must pass. Start dev server (`npm run dev`) and visit `/control-center/content` -- should show the tutorial table (requires being logged in as admin due to AdminGuard in layout).</verify>
  <done>The /control-center/content page shows a table with title, slug, date, and tags for all existing Building Blocks tutorials, replacing the placeholder content.</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes with no errors
2. `npm run build` succeeds with no TypeScript errors
3. The page is a server component (no "use client" directive)
4. getAllTutorials() is called to fetch tutorial data
5. Table renders title, slug, date, and tags for each tutorial
6. Tags are styled as pill badges
7. Empty state handled when no tutorials exist
</verification>

<success_criteria>
- Build and lint pass cleanly
- /control-center/content displays a real tutorial listing instead of placeholder text
- All four columns (title, slug, date, tags) are visible
- The page uses getAllTutorials() from the existing tutorials library
</success_criteria>

<output>
After completion, create `.planning/phases/18-content-editor-infrastructure/18-02-SUMMARY.md`
</output>
