---
phase: 01-scaffold-and-navigation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/layout/NavLinks.tsx
  - src/components/layout/Footer.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/projects/page.tsx
  - src/app/writing/page.tsx
  - src/app/assistant/page.tsx
  - src/app/contact/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees a navbar with links to Home, Projects, Writing, Assistant, and Contact"
    - "On mobile, visitor can open and close a hamburger menu smoothly"
    - "Visitor can identify which page they are on from the active navigation indicator"
    - "All pages render correctly on mobile, tablet, and desktop viewports"
    - "Clicking a mobile nav link closes the menu and navigates to the page"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Server component wrapper for navigation bar"
      contains: "NavLinks"
    - path: "src/components/layout/NavLinks.tsx"
      provides: "Client component with desktop nav, mobile hamburger menu, active link indicator"
      contains: "use client"
      exports: ["NavLinks"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Minimal footer server component"
      contains: "export"
    - path: "src/app/layout.tsx"
      provides: "Root layout importing Navbar and Footer"
      contains: "Navbar"
    - path: "src/app/projects/page.tsx"
      provides: "Projects stub page"
      contains: "Projects"
    - path: "src/app/writing/page.tsx"
      provides: "Writing stub page"
      contains: "Writing"
    - path: "src/app/assistant/page.tsx"
      provides: "Assistant stub page"
      contains: "Assistant"
    - path: "src/app/contact/page.tsx"
      provides: "Contact stub page"
      contains: "Contact"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Navbar.tsx"
      via: "import and render in layout"
      pattern: "import.*Navbar"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/components/layout/NavLinks.tsx"
      via: "import and render client component"
      pattern: "import.*NavLinks"
    - from: "src/components/layout/NavLinks.tsx"
      to: "next/navigation"
      via: "usePathname for active link detection"
      pattern: "usePathname"
    - from: "src/components/layout/NavLinks.tsx"
      to: "next/link"
      via: "Link component for client-side navigation"
      pattern: "import Link"
---

<objective>
Build the responsive navigation shell: Navbar with desktop links, mobile hamburger menu, active page indicator, page stubs for all 5 sections, and a minimal footer. This completes the Phase 1 goal of a navigable responsive site shell.

Purpose: Deliver the core navigation experience so visitors can move between all sections on any device.
Output: Working responsive navbar, 5 routable pages, and a layout shell.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-navigation/01-RESEARCH.md
@.planning/phases/01-scaffold-and-navigation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation components and page stubs</name>
  <files>
    src/components/layout/Navbar.tsx
    src/components/layout/NavLinks.tsx
    src/components/layout/Footer.tsx
    src/app/layout.tsx
    src/app/page.tsx
    src/app/projects/page.tsx
    src/app/writing/page.tsx
    src/app/assistant/page.tsx
    src/app/contact/page.tsx
  </files>
  <action>
Create the navigation components and page stubs following the architecture from research:

**1. `src/components/layout/NavLinks.tsx`** (Client Component -- the only "use client" file)

This is the core interactive component. It must:
- Add `"use client"` directive at the top
- Import `Link` from `next/link`, `usePathname` from `next/navigation`, `clsx` from `clsx`, `useState` from `react`
- Define a `links` array: `[{name: "Home", href: "/"}, {name: "Projects", href: "/projects"}, {name: "Writing", href: "/writing"}, {name: "Assistant", href: "/assistant"}, {name: "Contact", href: "/contact"}]`
- Implement `isActive` helper: exact match for "/" (home), `pathname.startsWith(href)` for all others
- Render **desktop nav** (hidden on mobile, visible md+): horizontal flex row of Link components with active styling (e.g., `text-blue-600 border-b-2 border-blue-600` for active, `text-gray-600 hover:text-gray-900` for inactive)
- Render **hamburger button** (visible on mobile, hidden md+): native `<button>` element with `aria-label` that toggles between "Open menu" and "Close menu", `aria-expanded={mobileOpen}`. Use a simple SVG hamburger icon (3 horizontal lines) that transforms to an X when open.
- Render **mobile menu** (conditionally shown when `mobileOpen` is true): full-width dropdown below the navbar with vertical link list. Each link calls `setMobileOpen(false)` on click to close menu after navigation. Active links get distinct styling (e.g., `text-blue-600 bg-blue-50`).
- Add smooth transition for mobile menu (use Tailwind transition classes like `transition-all duration-200`)

**2. `src/components/layout/Navbar.tsx`** (Server Component)

Simple wrapper:
- Import `NavLinks` from `./NavLinks`
- Render a `<header>` with a container div, Dan's name/logo text on the left (link to "/"), and `<NavLinks />` on the right
- Use `relative` positioning so mobile menu can position absolutely below
- Style: white background, subtle bottom border, horizontal padding, flex layout with space-between

**3. `src/components/layout/Footer.tsx`** (Server Component)

Minimal footer:
- Render a `<footer>` with centered text: copyright year and "Dan Weinbeck"
- Style: subtle top border, padding, muted text color
- Keep it simple -- this will be enhanced in later phases

**4. `src/app/layout.tsx`** (Update existing)

Modify the scaffold-generated layout:
- Import `Navbar` from `@/components/layout/Navbar`
- Import `Footer` from `@/components/layout/Footer`
- Update metadata: `title: "Dan Weinbeck"`, `description: "AI developer, analytics professional, and data scientist"`
- Structure body as: `<Navbar />` then `<main className="min-h-screen">{children}</main>` then `<Footer />`
- Do NOT add `"use client"` to layout.tsx -- it must remain a server component
- Keep the existing globals.css import

**5. `src/app/page.tsx`** (Update existing home page)

Replace scaffold default content with a minimal stub:
```tsx
export default function HomePage() {
  return (
    <div className="container mx-auto px-4 py-16">
      <h1 className="text-3xl font-bold">Dan Weinbeck</h1>
      <p className="mt-4 text-gray-600">AI developer, analytics professional, and data scientist.</p>
    </div>
  );
}
```

**6. Create 4 section page stubs** (Projects, Writing, Assistant, Contact)

Each follows the same pattern -- a simple server component with heading and "Coming soon" text:
- `src/app/projects/page.tsx` -- heading "Projects"
- `src/app/writing/page.tsx` -- heading "Writing"
- `src/app/assistant/page.tsx` -- heading "Assistant"
- `src/app/contact/page.tsx` -- heading "Contact"

Each stub should use the same container/padding pattern as the home page for consistency.
  </action>
  <verify>
Run these commands:

```bash
npm run build    # Build succeeds with all pages
npx biome check src/  # Linting passes
```

Then start the dev server and verify:

```bash
npm run dev
```

Check that:
- All 5 routes respond: `/`, `/projects`, `/writing`, `/assistant`, `/contact`
- The navbar renders with all 5 links
- The footer appears on every page
- `src/components/layout/NavLinks.tsx` contains `"use client"` and `usePathname`
- `src/app/layout.tsx` does NOT contain `"use client"`
  </verify>
  <done>
All 5 pages are routable. Navbar renders with desktop links (hidden on mobile) and hamburger button (hidden on desktop). NavLinks client component uses usePathname for active link detection. Footer appears on every page. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Responsive navigation shell with 5 pages: Home, Projects, Writing, Assistant, and Contact. Desktop navbar with active link indicator. Mobile hamburger menu with smooth open/close. Footer on every page.
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000 in your browser
2. **Desktop nav**: Verify all 5 links appear in the navbar. Click each link and confirm:
   - The page changes to the correct content (heading matches section name)
   - The active link is visually distinct (blue text, bottom border)
   - The navbar and footer persist across navigations
3. **Mobile nav**: Resize browser to mobile width (< 768px) or use DevTools responsive mode:
   - Verify the hamburger icon appears and desktop links are hidden
   - Click the hamburger -- menu should open smoothly showing all 5 links
   - Click a link -- menu should close and page should navigate
   - Open menu again and click the X/close button -- menu should close
4. **Tablet**: Check at ~768px width -- should show desktop nav layout
5. Verify the footer shows copyright text on every page
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All 5 routes return valid HTML: `/`, `/projects`, `/writing`, `/assistant`, `/contact`
3. Navbar visible on every page with all 5 links
4. Active page indicator highlights current page link
5. Mobile hamburger menu opens and closes
6. Mobile menu closes on link click (navigation)
7. Footer visible on every page
8. No `"use client"` in layout.tsx
9. `"use client"` present in NavLinks.tsx
</verification>

<success_criteria>
A visitor can navigate between all 5 sections using the navbar on desktop and the hamburger menu on mobile. The current page is visually indicated in the navigation. The layout (navbar + content + footer) is consistent across all pages. All pages render correctly on mobile, tablet, and desktop viewports.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-and-navigation/01-02-SUMMARY.md`
</output>
