---
phase: 02-home-page
plan: "01"
title: "Hero section with headshot, bio, CTAs, and animation foundations"
estimates:
  tasks: 3
  files_created: 2
  files_modified: 3
execution:
  wave: 1
  autonomous: true
artifacts:
  - path: next.config.ts
    type: modify
    description: "Add images.qualities config for Next.js 16 Image component"
  - path: src/app/globals.css
    type: modify
    description: "Add @theme animation keyframes for fade-in-up entrance"
  - path: src/types/project.ts
    type: create
    description: "Project interface matching future GitHub API shape"
  - path: src/components/home/HeroSection.tsx
    type: create
    description: "Hero section with headshot, name, tagline, bio, and CTA buttons"
  - path: src/app/page.tsx
    type: modify
    description: "Compose home page with HeroSection (FeaturedProjects + BlogTeaser added in Plan 02)"
must_haves:
  truths:
    - "Visitor sees Dan's headshot, name, tagline, and short bio above the fold"
    - "Visitor can click CTA buttons to reach Projects, Contact, GitHub, and LinkedIn"
    - "Headshot loads optimized via next/image with preload"
    - "Subtle fade-in animation plays on hero section (respects reduced-motion)"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero section with headshot, bio, CTAs"
      min_lines: 40
    - path: "src/types/project.ts"
      provides: "Project type for placeholder data"
      exports: ["Project"]
    - path: "next.config.ts"
      provides: "Image qualities config"
      contains: "qualities"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "src/components/home/HeroSection.tsx"
      to: "/headshot.jpeg"
      via: "next/image src prop"
      pattern: "src=\"/headshot.jpeg\""
---

<objective>
Build the hero section of Dan's home page with optimized headshot, bio content, CTA buttons, and set up the animation/config foundations that Plan 02 will also use.

Purpose: This is the above-the-fold experience -- the first thing visitors see. It must immediately communicate who Dan is and provide clear next actions.
Output: Working hero section visible at localhost:3000, plus Project type and animation keyframes ready for Plan 02.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-page/02-RESEARCH.md
@src/app/page.tsx
@src/app/layout.tsx
@src/app/globals.css
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config and foundation files</name>
  <files>next.config.ts, src/app/globals.css, src/types/project.ts</files>
  <action>
  **next.config.ts** -- Add `images.qualities` for Next.js 16 Image component:
  ```typescript
  import type { NextConfig } from "next";

  const nextConfig: NextConfig = {
    images: {
      qualities: [25, 50, 75, 100],
    },
  };

  export default nextConfig;
  ```
  Keep the existing comment about Biome if desired, or remove -- not critical.

  **src/app/globals.css** -- Add a `@theme` block with fade-in-up animation keyframes AFTER the existing `@theme inline` block. Do NOT remove or modify the existing `@theme inline` block or any existing CSS. Append:
  ```css
  @theme {
    --animate-fade-in-up: fade-in-up 0.5s ease-out both;

    @keyframes fade-in-up {
      from {
        opacity: 0;
        transform: translateY(0.5rem);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  }
  ```

  **src/types/project.ts** -- Create the Project interface. This type will be used by placeholder data in Plan 02 and replaced with real GitHub API data in Phase 3:
  ```typescript
  export interface Project {
    name: string;
    description: string;
    language: string | null;
    stars: number;
    url: string;
    topics: string[];
  }
  ```
  </action>
  <verify>
  Run `npx next build` -- should complete without errors. The new @theme block should not conflict with the existing one.
  Run `npx @biomejs/biome check src/types/project.ts` -- should pass.
  </verify>
  <done>next.config.ts has images.qualities array, globals.css has fade-in-up keyframes, src/types/project.ts exports Project interface.</done>
</task>

<task type="auto">
  <name>Task 2: HeroSection component</name>
  <files>src/components/home/HeroSection.tsx</files>
  <action>
  Create `src/components/home/HeroSection.tsx` as a **server component** (no "use client" directive).

  Layout structure (matches wireframe):
  - Outer `<section>` with `py-16 md:py-24` and `motion-safe:animate-fade-in-up`
  - Flex container: `flex flex-col md:flex-row items-center gap-8 md:gap-12`
  - Left: Headshot using `next/image` -- `src="/headshot.jpeg"`, `alt="Dan Weinbeck"`, `width={160}`, `height={160}`, `preload` (NOT `priority`), `className="rounded-full object-cover"`
  - Right: Text content, centered on mobile (`text-center md:text-left`)
    - `<h1>` with `text-3xl sm:text-4xl font-bold text-gray-900`: "Dan Weinbeck"
    - `<p>` with `mt-2 text-lg text-gray-600`: "Self-taught AI developer \u00b7 analytics pro \u00b7 data scientist" (use middle dot \u00b7 as separator)
    - `<p>` with `mt-4 max-w-lg text-gray-600`: "I build practical AI agents and data products that ship. Interests in experimentation, UX, automation, and side projects."
    - CTA button row: `mt-6 flex flex-wrap justify-center md:justify-start gap-3`

  CTA buttons (4 total):
  1. **View Projects** (primary, filled) -- Use `next/link` (`import Link from "next/link"`), `href="/projects"`, classes: `inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 transition-colors`
  2. **Contact** (secondary, outlined) -- Use `next/link`, `href="/contact"`, classes: `inline-flex items-center px-4 py-2 border border-gray-300 text-gray-700 text-sm font-medium rounded-md hover:bg-gray-50 transition-colors`
  3. **GitHub** (icon button, external) -- Use `<a>` tag (NOT next/link), `href="https://github.com/dweinbeck"`, `target="_blank" rel="noopener noreferrer"`, `aria-label="GitHub"`, classes: `inline-flex items-center justify-center h-10 w-10 border border-gray-300 text-gray-600 rounded-md hover:bg-gray-50 hover:text-gray-900 transition-colors`. Include inline SVG for GitHub icon (simple path: `M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795 .945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z` viewBox `0 0 24 24` fill `currentColor` with `h-5 w-5`)
  4. **LinkedIn** (icon button, external) -- Same pattern as GitHub. `href="https://www.linkedin.com/in/dw789/"`, `aria-label="LinkedIn"`. LinkedIn SVG path: `M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z` viewBox `0 0 24 24` fill `currentColor` with `h-5 w-5`

  IMPORTANT:
  - Use `preload` NOT `priority` on the Image component (Next.js 16)
  - External links use `<a>` with `target="_blank" rel="noopener noreferrer"`, NOT next/link
  - Internal links (Projects, Contact) use `next/link` Link component
  - All hardcoded light-mode colors (matching existing Navbar/Footer pattern)
  - The `motion-safe:animate-fade-in-up` on the section respects reduced-motion preferences
  </action>
  <verify>
  Run `npx @biomejs/biome check src/components/home/HeroSection.tsx` -- should pass.
  Run `npx next build` -- should complete without errors.
  </verify>
  <done>HeroSection.tsx exists, renders headshot with next/image preload, shows name/tagline/bio, has 4 CTA buttons (2 internal Link, 2 external anchor), uses fade-in-up animation with motion-safe prefix.</done>
</task>

<task type="auto">
  <name>Task 3: Compose home page with HeroSection</name>
  <files>src/app/page.tsx</files>
  <action>
  Replace the current stub content in `src/app/page.tsx` with the section composition pattern. For now, only HeroSection is ready (Plan 02 adds FeaturedProjects and BlogTeaser):

  ```typescript
  import { HeroSection } from "@/components/home/HeroSection";

  export default function HomePage() {
    return (
      <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
        <HeroSection />
      </div>
    );
  }
  ```

  Keep the container pattern (`max-w-5xl px-4 sm:px-6 lg:px-8`) consistent with existing codebase.
  </action>
  <verify>
  Run `npx next build` -- should complete without errors.
  Run `npx @biomejs/biome check src/app/page.tsx` -- should pass.
  Start dev server (`npx next dev`) and verify at http://localhost:3000:
  - Headshot displays as a circle
  - Name, tagline, and bio text visible
  - 4 CTA buttons render (View Projects, Contact, GitHub icon, LinkedIn icon)
  - View Projects links to /projects, Contact links to /contact
  - GitHub and LinkedIn open in new tabs to correct URLs
  </verify>
  <done>Home page renders HeroSection with headshot, bio, and all 4 CTA buttons. Page builds without errors. Internal links use next/link, external links use anchor tags with security attributes.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npx @biomejs/biome check src/` passes
3. Dev server shows hero section at localhost:3000 with headshot, name, tagline, bio, and 4 CTA buttons
4. Headshot renders as optimized circle (next/image with preload)
5. GitHub and LinkedIn buttons open correct URLs in new tabs
6. View Projects and Contact buttons navigate to correct internal routes
</verification>

<success_criteria>
- Hero section is visible above the fold at localhost:3000
- Headshot loads via next/image with preload attribute (not priority)
- All 4 CTA buttons work (2 internal links, 2 external links with target="_blank" rel="noopener noreferrer")
- Fade-in-up animation plays on load (respects prefers-reduced-motion)
- next.config.ts has images.qualities configured
- Project type exists at src/types/project.ts ready for Plan 02
- Build passes, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-01-SUMMARY.md`
</output>
