---
phase: 19.1-custom-gpts-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/custom-gpts.json
  - src/lib/custom-gpts.ts
  - src/app/custom-gpts/page.tsx
  - src/components/layout/NavLinks.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /custom-gpts renders a page titled 'Custom GPTs' with SEO metadata"
    - "The page displays a responsive 2-column card grid with 4 placeholder GPT entries"
    - "Each card links to an external placeholder URL that opens in a new tab"
    - "Card styling (title, description, tags, CTA) is visually consistent with Building Blocks TutorialCard"
    - "'Custom GPTs' appears in the site navigation between 'Building Blocks' and 'Assistant'"
    - "The nav entry shows active-state styling when on the /custom-gpts route"
    - "Build, lint, and type checks pass with no regressions"
  artifacts:
    - path: "src/data/custom-gpts.json"
      provides: "GPT entry data (4 placeholder entries with slug, name, description, url, tags)"
      contains: "PLACEHOLDER"
    - path: "src/lib/custom-gpts.ts"
      provides: "CustomGpt interface and getCustomGpts() loader function"
      exports: ["CustomGpt", "getCustomGpts"]
    - path: "src/app/custom-gpts/page.tsx"
      provides: "Server component page with Metadata export and card grid"
      exports: ["metadata", "default"]
    - path: "src/components/layout/NavLinks.tsx"
      provides: "Navigation with Custom GPTs entry"
      contains: "Custom GPTs"
  key_links:
    - from: "src/app/custom-gpts/page.tsx"
      to: "src/lib/custom-gpts.ts"
      via: "import getCustomGpts"
      pattern: "import.*getCustomGpts.*from.*custom-gpts"
    - from: "src/lib/custom-gpts.ts"
      to: "src/data/custom-gpts.json"
      via: "JSON import"
      pattern: "import.*from.*data/custom-gpts.json"
    - from: "src/app/custom-gpts/page.tsx"
      to: "src/components/ui/Card.tsx"
      via: "Card component with external href"
      pattern: "Card.*variant.*clickable.*href.*gpt.url"
    - from: "src/components/layout/NavLinks.tsx"
      to: "/custom-gpts"
      via: "baseLinks array entry"
      pattern: "Custom GPTs.*custom-gpts"
---

<objective>
Create the Custom GPTs public page with a data-driven card grid, typed loader, and navigation entry -- all following established codebase patterns.

Purpose: Give visitors a way to discover and try Dan's Custom GPTs, consistent with the site's Projects and Building Blocks pages.
Output: A new `/custom-gpts` route with 4 placeholder GPT cards, a typed data file for easy editing, and a nav bar entry.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-custom-gpts-page/19.1-RESEARCH.md

# Key reference files (read these before implementing)
@src/data/accomplishments.json — data file pattern (JSON array with slugs, titles, descriptions, skills)
@src/lib/accomplishments.ts — typed loader pattern (interface + getXxx() function)
@src/app/building-blocks/page.tsx — page layout pattern (metadata, max-w-5xl container, responsive grid)
@src/components/building-blocks/TutorialCard.tsx — card content styling pattern (title, description, tags, CTA)
@src/components/ui/Card.tsx — Card component (variant="clickable", external link handling)
@src/components/layout/NavLinks.tsx — navigation pattern (baseLinks array, isActive function)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data file and typed loader</name>
  <files>src/data/custom-gpts.json, src/lib/custom-gpts.ts</files>
  <action>
    **1. Create `src/data/custom-gpts.json`** with 4 placeholder GPT entries.

    Each entry has: `slug` (string), `name` (string), `description` (string), `url` (string), `tags` (string array).

    Use these 4 entries covering varied topics:
    ```json
    [
      {
        "slug": "code-review-assistant",
        "name": "Code Review Assistant",
        "description": "Analyzes pull requests and code snippets for best practices, potential bugs, and performance improvements. Provides actionable feedback with clear explanations.",
        "url": "https://chat.openai.com/g/g-PLACEHOLDER-code-review-assistant",
        "tags": ["Code Review", "Developer Tools"]
      },
      {
        "slug": "data-storyteller",
        "name": "Data Storyteller",
        "description": "Transforms raw data insights into compelling narratives for stakeholders. Helps craft data-driven presentations, reports, and executive summaries.",
        "url": "https://chat.openai.com/g/g-PLACEHOLDER-data-storyteller",
        "tags": ["Data", "Communication"]
      },
      {
        "slug": "sql-query-builder",
        "name": "SQL Query Builder",
        "description": "Generates optimized SQL queries from natural language descriptions. Supports PostgreSQL, MySQL, and BigQuery dialects with query explanation.",
        "url": "https://chat.openai.com/g/g-PLACEHOLDER-sql-query-builder",
        "tags": ["SQL", "Databases"]
      },
      {
        "slug": "career-coach",
        "name": "Career Coach",
        "description": "Provides personalized career guidance for analytics and tech professionals. Helps with resume reviews, interview preparation, and career transition strategies.",
        "url": "https://chat.openai.com/g/g-PLACEHOLDER-career-coach",
        "tags": ["Career", "Productivity"]
      }
    ]
    ```

    Use `PLACEHOLDER` in URLs to make them obviously not real (matching Pitfall 3 from research).

    **2. Create `src/lib/custom-gpts.ts`** following the `accomplishments.ts` pattern exactly:

    ```typescript
    import customGptsData from "@/data/custom-gpts.json";

    export interface CustomGpt {
      slug: string;
      name: string;
      description: string;
      url: string;
      tags: string[];
    }

    export function getCustomGpts(): CustomGpt[] {
      return customGptsData as CustomGpt[];
    }
    ```

    This mirrors `src/lib/accomplishments.ts`: JSON import, exported interface, typed getter function.
  </action>
  <verify>
    - `npx tsc --noEmit` passes (no type errors)
    - The JSON file is valid JSON (no trailing commas, correct structure)
    - The loader exports both the interface and the getter function
  </verify>
  <done>
    - `src/data/custom-gpts.json` exists with 4 entries, each having slug/name/description/url/tags
    - All URLs contain "PLACEHOLDER" for easy identification
    - `src/lib/custom-gpts.ts` exports `CustomGpt` interface and `getCustomGpts()` function
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create page component and add navigation entry</name>
  <files>src/app/custom-gpts/page.tsx, src/components/layout/NavLinks.tsx</files>
  <action>
    **1. Create `src/app/custom-gpts/page.tsx`** as a server component (NO `"use client"`).

    Follow the `building-blocks/page.tsx` layout pattern exactly:
    - Export `metadata: Metadata` with `title: "Custom GPTs"` and a description
    - Container: `mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 py-16`
    - Heading: `text-3xl font-bold text-text-primary`
    - Subheading: `mt-2 text-text-secondary`
    - Grid: `mt-10 grid grid-cols-1 md:grid-cols-2 gap-6`

    For each GPT entry, render a `Card` component with:
    - `variant="clickable"` and `href={gpt.url}` (Card handles external link attributes automatically)
    - **CRITICAL:** Pass `className="group"` to the Card component so that `group-hover` classes on child elements work. The Card component does NOT add `group` by itself.
    - Card children styled consistently with TutorialCard:
      - Title: `<h3>` with `text-lg font-bold text-text-primary group-hover:text-gold transition-colors duration-200`
      - Description: `<p>` with `mt-3 text-sm text-text-secondary leading-relaxed line-clamp-3`
      - Tags: `<div>` with `mt-5 flex flex-wrap gap-2`, each tag `<span>` with `px-2.5 py-0.5 font-mono text-xs text-text-tertiary bg-[rgba(27,42,74,0.04)] rounded-full`
      - CTA: `<span>` with `mt-5 inline-block text-sm font-medium text-gold group-hover:text-gold-hover transition-colors duration-200` and text "Open GPT →" (use `&rarr;` entity)

    Do NOT use `font-[family-name:var(--font-display)]` on the title (TutorialCard uses it for Playfair Display, but GPT names are shorter and work better with the default Inter font).

    **2. Edit `src/components/layout/NavLinks.tsx`** to add "Custom GPTs" to `baseLinks` array.

    Insert the entry AFTER "Building Blocks" and BEFORE "Assistant":
    ```typescript
    const baseLinks = [
      { name: "Home", href: "/" },
      { name: "About", href: "/about" },
      { name: "Projects", href: "/projects" },
      { name: "Building Blocks", href: "/building-blocks" },
      { name: "Custom GPTs", href: "/custom-gpts" },  // NEW
      { name: "Assistant", href: "/assistant" },
      { name: "Contact", href: "/contact" },
    ];
    ```

    The existing `isActive()` function already handles non-root routes via `pathname.startsWith(href)`, so `/custom-gpts` will automatically highlight correctly.
  </action>
  <verify>
    - `npm run build` succeeds (page compiles, no errors)
    - `npm run lint` passes (Biome v2.3 clean)
    - `npm test` passes (no regressions)
    - Visit `/custom-gpts` in dev — page renders with 4 cards in a 2-column grid
    - Clicking a card opens a new tab (target="_blank" via Card component)
    - "Custom GPTs" appears in navigation between "Building Blocks" and "Assistant"
    - Active state styling works on `/custom-gpts` route
  </verify>
  <done>
    - `/custom-gpts` route renders a page titled "Custom GPTs" with SEO metadata
    - Page shows 4 cards in a responsive 2-column grid (1-col on mobile)
    - Each card displays name, description, tags, and "Open GPT →" CTA
    - Cards link to external placeholder URLs opening in new tabs
    - "Custom GPTs" appears in the nav between "Building Blocks" and "Assistant"
    - Nav active state works correctly on the /custom-gpts route
    - Build, lint, and type checks pass with no regressions
  </done>
</task>

</tasks>

<verification>
Run all quality gates:
```bash
npm test && npm run lint && npm run build
```

All three must pass. If any fails, fix before completing.

Visual spot-check (manual):
1. `npm run dev` → visit `http://localhost:3000/custom-gpts`
2. Verify 4 cards in 2-column grid on desktop, 1-column on mobile
3. Verify card hover effects (shadow lift, gold border, title color change, CTA color change)
4. Verify clicking a card opens new tab
5. Verify "Custom GPTs" in nav with correct active state
6. Verify page title in browser tab shows "Custom GPTs | Dan Weinbeck"
</verification>

<success_criteria>
1. `/custom-gpts` route exists and renders with SEO metadata (title + description)
2. Page displays a responsive card grid (2-col desktop, 1-col mobile) with 4 GPT cards
3. Card styling matches Building Blocks / TutorialCard visual conventions
4. Each card links externally to a placeholder URL (target="_blank", rel="noopener noreferrer")
5. Data comes from `src/data/custom-gpts.json` with 4 entries containing "PLACEHOLDER" URLs
6. "Custom GPTs" appears in navigation between "Building Blocks" and "Assistant"
7. Nav active state works on `/custom-gpts` route
8. `npm test && npm run lint && npm run build` all pass
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-custom-gpts-page/19.1-01-SUMMARY.md`
</output>
