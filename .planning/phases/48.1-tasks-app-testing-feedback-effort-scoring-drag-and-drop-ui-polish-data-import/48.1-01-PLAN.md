---
phase: 48.1-tasks-app-testing-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tasks/sidebar.tsx
  - src/components/tasks/section-header.tsx
  - src/app/apps/tasks/(authenticated)/[projectId]/project-view.tsx
  - src/app/apps/tasks/(authenticated)/tags/tag-list.tsx
  - src/app/apps/tasks/(authenticated)/search/search-input.tsx
  - src/components/tasks/ui/help-tip.tsx
  - src/data/help-tips.ts
  - src/lib/hooks/use-tooltip-position.ts
autonomous: true
requirements: [TF-04, TF-05]

must_haves:
  truths:
    - "No ? help tip icons appear anywhere in the Tasks app"
    - "Sidebar has a 'Home' link that navigates to /apps/tasks"
    - "Home link is always visible in the sidebar navigation"
  artifacts:
    - path: "src/components/tasks/sidebar.tsx"
      provides: "Sidebar with Home nav item added"
      contains: "Home"
  key_links:
    - from: "src/components/tasks/sidebar.tsx"
      to: "/apps/tasks"
      via: "Link href"
      pattern: "href.*\\/apps\\/tasks"
---

<objective>
Remove all HelpTip (? icon) components from the Tasks app and add a "Home" link to the sidebar navigation.

Purpose: Clean up the UI by removing help tip icons that are no longer needed (demo will be added later), and provide users a way to navigate back to the /apps/tasks welcome page from anywhere in the app.
Output: Tasks app with no ? icons and a persistent Home link in the sidebar.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/tasks/sidebar.tsx
@src/components/tasks/ui/help-tip.tsx
@src/data/help-tips.ts
@src/components/tasks/section-header.tsx
@src/app/apps/tasks/(authenticated)/[projectId]/project-view.tsx
@src/app/apps/tasks/(authenticated)/tags/tag-list.tsx
@src/app/apps/tasks/(authenticated)/search/search-input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all HelpTip usages and clean up help tip infrastructure</name>
  <files>
    src/components/tasks/sidebar.tsx
    src/components/tasks/section-header.tsx
    src/app/apps/tasks/(authenticated)/[projectId]/project-view.tsx
    src/app/apps/tasks/(authenticated)/tags/tag-list.tsx
    src/app/apps/tasks/(authenticated)/search/search-input.tsx
  </files>
  <action>
    Remove all `<HelpTip>` component usages and their imports from these 5 files:

    1. **sidebar.tsx**: Remove the `<HelpTip tipId="quick-add" />` from the Add Task button area (line ~167). Remove the import of HelpTip.
    2. **section-header.tsx**: Remove `<HelpTip tipId="sections" />` from the section name area (line ~102). Remove the import of HelpTip.
    3. **project-view.tsx**: Remove `<HelpTip tipId="board-view" />` from the view mode toggle area (line ~98). Remove the import of HelpTip.
    4. **tag-list.tsx**: Remove `<HelpTip tipId="tags" />` from the manage tags header (line ~67). Remove the import of HelpTip. Keep the "Manage tags" text span.
    5. **search-input.tsx**: Remove `<HelpTip tipId="search" />` from the search header (line ~28). Remove the import of HelpTip. Keep the "Search tasks" text span.

    Then delete the now-unused files:
    - `src/components/tasks/ui/help-tip.tsx` — the component itself
    - `src/data/help-tips.ts` — the help tips data
    - `src/lib/hooks/use-tooltip-position.ts` — the tooltip positioning hook (only used by HelpTip)

    Verify no other files import from these deleted files by grepping for "help-tip", "help-tips", and "use-tooltip-position".
  </action>
  <verify>
    - `grep -r "HelpTip" src/` returns no results
    - `grep -r "help-tip" src/` returns no results (no imports of deleted files)
    - `grep -r "use-tooltip-position" src/` returns no results
    - `npm run lint` passes
    - `npm run build` passes
  </verify>
  <done>No ? help tip icons appear anywhere in the Tasks app. The HelpTip component, its data file, and the tooltip positioning hook are all deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Add Home link to sidebar navigation</name>
  <files>src/components/tasks/sidebar.tsx</files>
  <action>
    Add a "Home" navigation item to the sidebar, positioned as the FIRST item in the `navItems` array. This ensures it's always visible at the top of the sidebar nav.

    In `src/components/tasks/sidebar.tsx`:

    1. Add a new entry at the START of the `navItems` array:
       ```
       { href: "/apps/tasks", label: "Home", icon: "home" }
       ```

    2. Add a "home" case to the `NavIcon` switch statement with an SVG home icon:
       ```tsx
       case "home":
         return (
           <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
             <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
             <polyline points="9 22 9 12 15 12 15 22" />
           </svg>
         );
       ```

    3. The existing nav rendering logic uses `pathname === item.href` for active state highlighting. For the Home link, this will correctly highlight ONLY when the user is on `/apps/tasks` exactly (not sub-pages), which is the desired behavior.
  </action>
  <verify>
    - `npm run build` passes
    - The sidebar nav items array includes "Home" as the first item
    - The NavIcon switch has a "home" case
  </verify>
  <done>Sidebar has a "Home" link as the first navigation item. Clicking it navigates to /apps/tasks welcome page. It's always visible regardless of which project the user is viewing.</done>
</task>

</tasks>

<verification>
- `npm run lint && npm run build && npm test` — all pass
- No HelpTip references in any source file
- Sidebar shows Home, Today, Completed, Search, Filters & Tags
- Home link href is /apps/tasks
</verification>

<success_criteria>
1. Zero ? help tip icons rendered anywhere in the Tasks app
2. Sidebar has a Home link at the top of the navigation that goes to /apps/tasks
3. Build, lint, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-tasks-app-testing-feedback-effort-scoring-drag-and-drop-ui-polish-data-import/48.1-01-SUMMARY.md`
</output>
