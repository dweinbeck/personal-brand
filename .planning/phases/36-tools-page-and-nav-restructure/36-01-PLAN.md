---
phase: 36-tools-page-and-nav-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/tools.ts
  - src/app/tools/page.tsx
  - src/data/apps.ts
  - src/app/apps/page.tsx
  - src/app/custom-gpts/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /tools shows a card grid with New Phase Planner, FRD Interviewer, FRD Generator, Research Assistant, and Digital Envelopes"
    - "Research Assistant and Digital Envelopes no longer appear on /apps"
    - "/custom-gpts redirects to /tools (no broken bookmarks)"
  artifacts:
    - path: "src/data/tools.ts"
      provides: "Tool listing data for the Tools page"
      exports: ["ToolListing", "getTools"]
    - path: "src/app/tools/page.tsx"
      provides: "Tools index page with card grid"
      contains: "getTools"
    - path: "src/app/custom-gpts/page.tsx"
      provides: "Redirect from /custom-gpts to /tools"
      contains: "redirect"
  key_links:
    - from: "src/app/tools/page.tsx"
      to: "src/data/tools.ts"
      via: "getTools import"
      pattern: "import.*getTools.*from.*tools"
    - from: "src/data/apps.ts"
      to: "none"
      via: "envelopes and research-assistant removed"
      pattern: "brand-scraper|tasks"
---

<objective>
Create the /tools page with its data source, move Research Assistant and Digital Envelopes off the /apps page, and set up a redirect from the old /custom-gpts URL.

Purpose: NAV-01 requires a clear distinction between Apps (full applications) and Tools (single-function dev utilities). This plan creates the Tools page content and removes moved items from Apps.
Output: Working /tools page with 5 tool cards, slimmed /apps page with 2 app cards, /custom-gpts redirect
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/data/custom-gpts.json
@src/lib/custom-gpts.ts
@src/app/custom-gpts/page.tsx
@src/data/apps.ts
@src/app/apps/page.tsx
@src/components/apps/AppCard.tsx
@src/components/ui/Card.tsx
@src/components/ui/CardButtonLabel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools data source and /tools index page</name>
  <files>src/data/tools.ts, src/app/tools/page.tsx</files>
  <action>
1. Create `src/data/tools.ts` with a `ToolListing` interface and `getTools()` function. The interface should have: `slug`, `title`, `tag`, `subtitle`, `description`, `href` (string, internal route or external URL), and `external` (boolean, true if href is an external ChatGPT link).

Define these 5 tools in this order:
- **New Phase Planner**: tag "Planning", subtitle "Plan product phases with structured AI guidance", description "When you have an idea for more than a trivial set of updates to your product, use this GPT to break it into actionable phases.", href `https://chatgpt.com/g/g-69895e8109a881919e4012452f0a7cd8-new-phase-planner`, external true
- **FRD Interviewer**: tag "Requirements", subtitle "Generate requirements through guided conversation", description "This GPT asks you questions until it has enough information to create a simple but comprehensive functional requirements document.", href `https://chatgpt.com/g/g-6987d85cd11081918ff321e9dc927966-frd-interviewer`, external true
- **FRD Generator**: tag "Requirements", subtitle "Create functional requirements documents from specs", description "Provide your project specifications and get a structured FRD document with user stories, acceptance criteria, and technical requirements.", href `https://chatgpt.com/g/g-frd-generator` (placeholder URL - use this for now), external true
- **Research Assistant**: tag "AI", subtitle "Compare AI models side-by-side in real time", description "Send a prompt to two AI models simultaneously and see their responses stream side-by-side. Choose between Standard and Expert tiers for different model combinations.", href `/tools/research-assistant`, external false
- **Digital Envelopes**: tag "Finance", subtitle "Zero-based budgeting with digital envelopes", description "Manage your monthly budget using the envelope budgeting method. Allocate income to categories and track spending in real time.", href `/envelopes`, external false

2. Create `src/app/tools/page.tsx` as a server component with metadata (title: "Tools", description: "Single-function development utilities and AI-powered tools built by Dan Weinbeck."). Import `getTools` from `@/data/tools`. Import `Card` from `@/components/ui/Card` and `CardButtonLabel` from `@/components/ui/CardButtonLabel`.

Render a page matching the existing Custom GPTs page layout pattern:
- Max-w-5xl container, py-16
- h1 "Tools" with the same text-3xl font-bold text-text-primary styling
- Subtitle paragraph: "Single-function dev utilities and AI-powered tools for analytics, development, and planning."
- Grid: mt-10 grid grid-cols-1 md:grid-cols-2 gap-6
- Each card: Use `Card` component with variant="clickable", same styling as AppCard (tag badge with gold-light styling, title, subtitle, description with line-clamp-3, and CardButtonLabel at bottom).
- For the CardButtonLabel text: use "Open Tool" for external links and "Enter App" for internal links.
- For external links, the Card href should open in a new tab. Check if Card supports `target="_blank"` — if it wraps an anchor tag with href, add target and rel attributes. If Card does not support target prop, wrap the card content in an anchor tag or adjust accordingly. The key is that external GPT links open in a new tab.
  </action>
  <verify>
Run `npm run build` — page compiles without errors. Verify `src/data/tools.ts` exports 5 tool entries. Verify `src/app/tools/page.tsx` imports and renders them.
  </verify>
  <done>
/tools page renders 5 tool cards in a 2-column grid: New Phase Planner, FRD Interviewer, FRD Generator, Research Assistant, and Digital Envelopes. External GPT links open in new tabs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove moved items from /apps and redirect /custom-gpts</name>
  <files>src/data/apps.ts, src/app/custom-gpts/page.tsx</files>
  <action>
1. Edit `src/data/apps.ts`: Remove the "envelopes" and "research-assistant" entries from the `getApps()` return array. Only "brand-scraper" and "tasks" should remain. Keep the AppListing interface and getApps function signature unchanged.

2. Replace `src/app/custom-gpts/page.tsx` with a server-side redirect to /tools. Use Next.js `redirect()` from `next/navigation`:
```tsx
import { redirect } from "next/navigation";

export default function CustomGptsPage() {
  redirect("/tools");
}
```
Remove the metadata export and all old imports (Card, CardButtonLabel, getCustomGpts) since the page now just redirects.

Note: Do NOT delete `src/lib/custom-gpts.ts` or `src/data/custom-gpts.json` yet — they may be referenced elsewhere and can be cleaned up in a future phase. The redirect handles the user-facing concern.
  </action>
  <verify>
Run `npm run build` — no errors. Verify `src/data/apps.ts` returns exactly 2 apps (brand-scraper, tasks). Verify `src/app/custom-gpts/page.tsx` contains `redirect("/tools")`.
  </verify>
  <done>
/apps page shows only Brand Scraper and Tasks (no Envelopes, no Research). Visiting /custom-gpts redirects to /tools with no 404.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes with zero errors
3. /tools page exists and shows 5 tool cards
4. /apps page shows only 2 app cards (Brand Scraper, Tasks)
5. /custom-gpts redirects to /tools
</verification>

<success_criteria>
- /tools renders New Phase Planner, FRD Interviewer, FRD Generator, Research Assistant, Digital Envelopes in a card grid
- /apps renders only Brand Scraper and Tasks
- /custom-gpts redirects to /tools
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-tools-page-and-nav-restructure/36-01-SUMMARY.md`
</output>
