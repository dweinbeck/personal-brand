---
phase: 01-testing-feedback-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/tools.ts
  - src/app/tools/page.tsx
  - src/components/home/ToolsShowcase.tsx
  - src/content/building-blocks/frd.mdx
  - src/content/building-blocks/_frd-fast.mdx
autonomous: true
requirements: []

must_haves:
  truths:
    - "FRD Generator links across the site point to dev.dan-weinbeck.com/tools/frd-generator (not chatgpt.com)"
    - "Tools page subtitle spells out 'development' in full (no abbreviation)"
    - "Custom GPT tools (New Phase Planner, FRD Interviewer) show 'Open Custom GPT' button label"
    - "Standalone app tools (FRD Generator) show 'Open App' button label"
    - "Building blocks FRD article links to the standalone FRD Generator app"
  artifacts:
    - path: "src/data/tools.ts"
      provides: "Updated FRD Generator href and tool type classification"
    - path: "src/app/tools/page.tsx"
      provides: "Full 'development' spelling and dynamic button labels"
    - path: "src/components/home/ToolsShowcase.tsx"
      provides: "Dynamic button labels matching tool type"
    - path: "src/content/building-blocks/frd.mdx"
      provides: "Updated FRD Generator link to standalone app"
    - path: "src/content/building-blocks/_frd-fast.mdx"
      provides: "Updated FRD Generator link to standalone app"
  key_links:
    - from: "src/app/tools/page.tsx"
      to: "src/data/tools.ts"
      via: "getTools() call"
      pattern: "getTools\\(\\)"
    - from: "src/components/home/ToolsShowcase.tsx"
      to: "src/data/tools.ts"
      via: "getTools() call"
      pattern: "getTools\\(\\)"
---

<objective>
Fix FRD Generator links, tools page subtitle, and button labels across Tools page, home ToolsShowcase, and Building Blocks content.

Purpose: Address 4 testing feedback items (5.1, 7.1, 7.2, 7.3) for Tools page and Building Blocks.
Output: Correct FRD Generator URLs, proper subtitle text, and context-appropriate button labels.
</objective>

<execution_context>
@/Users/dweinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dweinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/tools.ts
@src/app/tools/page.tsx
@src/components/home/ToolsShowcase.tsx
@src/content/building-blocks/frd.mdx
@src/content/building-blocks/_frd-fast.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tools data and page UI</name>
  <files>
    src/data/tools.ts
    src/app/tools/page.tsx
    src/components/home/ToolsShowcase.tsx
  </files>
  <action>
**src/data/tools.ts** -- Update the tool data model and FRD Generator entry:
1. Add a `type` field to the `ToolListing` interface: `type: "custom-gpt" | "app"`. This distinguishes Custom GPTs from standalone apps.
2. Update each tool entry:
   - `new-phase-planner`: `type: "custom-gpt"` (keep existing href, external: true)
   - `frd-interviewer`: `type: "custom-gpt"` (keep existing href, external: true)
   - `frd-generator`: Change `href` from `"https://chatgpt.com/g/g-frd-generator"` to `"https://dev.dan-weinbeck.com/tools/frd-generator"`. Set `type: "app"`, keep `external: true` (it's a different domain/app).
3. Add a helper function `getToolButtonLabel(tool: ToolListing): string` that returns:
   - `"Open Custom GPT"` when `tool.type === "custom-gpt"`
   - `"Open App"` when `tool.type === "app"`

**src/app/tools/page.tsx** -- Fix subtitle and button labels:
1. Change the subtitle from `"Single-function dev utilities and AI-powered tools for analytics, development, and planning."` to `"Single-function development utilities and AI-powered tools for analytics, development, and planning."` (spell out "dev" to "development").
2. Replace the CardButtonLabel content from `{tool.external ? "Open Tool" : "Enter App"}` with the appropriate label using the new `getToolButtonLabel` function (or inline ternary based on `tool.type`):
   - `tool.type === "custom-gpt"` -> `"Open Custom GPT"`
   - `tool.type === "app"` -> `"Open App"`
   - fallback (no type) -> `"Open Tool"`

**src/components/home/ToolsShowcase.tsx** -- Update button labels to match:
1. Same button label logic as the tools page: replace `{tool.external ? "Open Tool" : "Enter App"}` with `tool.type === "custom-gpt" ? "Open Custom GPT" : tool.type === "app" ? "Open App" : "Open Tool"`.
  </action>
  <verify>
Run `npm run lint && npm run build` to verify no type errors or lint issues. Verify that:
1. Tools page subtitle contains "development" (not "dev")
2. New Phase Planner and FRD Interviewer cards show "Open Custom GPT"
3. FRD Generator card shows "Open App" and links to dev.dan-weinbeck.com/tools/frd-generator
  </verify>
  <done>
Tools page has full "development" in subtitle. Each tool card has the correct button label. FRD Generator points to the standalone app URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Building Blocks FRD content links</name>
  <files>
    src/content/building-blocks/frd.mdx
    src/content/building-blocks/_frd-fast.mdx
  </files>
  <action>
**src/content/building-blocks/frd.mdx** -- Update FRD Generator link:
1. Find the line: `- [FRD Generator](https://chatgpt.com/g/g-frd-generator) -- generate a structured FRD from your specs`
2. Replace the URL with `https://dev.dan-weinbeck.com/tools/frd-generator`

**src/content/building-blocks/_frd-fast.mdx** -- Update FRD Generator link:
1. Find the line: `[FRD Generator](https://chatgpt.com/g/g-frd-generator)`
2. Replace the URL with `https://dev.dan-weinbeck.com/tools/frd-generator`
  </action>
  <verify>
Run `npm run build` to verify MDX compilation succeeds. Grep for old chatgpt.com frd-generator URLs to confirm none remain:
`grep -r "chatgpt.com/g/g-frd-generator" src/`
Should return zero results.
  </verify>
  <done>
All FRD Generator links in Building Blocks content point to dev.dan-weinbeck.com/tools/frd-generator. No stale chatgpt.com FRD Generator URLs remain in the codebase.
  </done>
</task>

</tasks>

<verification>
1. `npm run lint` passes with zero errors
2. `npm run build` succeeds
3. `npm test` passes
4. No `chatgpt.com/g/g-frd-generator` URLs remain in `src/`
5. Tools page subtitle says "development" not "dev"
6. Button labels match tool type: "Open Custom GPT" or "Open App"
</verification>

<success_criteria>
All 4 Tools/Building Blocks feedback items addressed: FRD Generator links updated, subtitle spelled out, button labels differentiated by tool type.
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-feedback-md/01-02-SUMMARY.md`
</output>
