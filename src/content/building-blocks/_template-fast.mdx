{/* Fast companion for _template.mdx */}
{/* This file is the "Just give me the building block" tab. */}
{/* Name it _<slug>-fast.mdx to pair with <slug>.mdx automatically. */}
{/* No metadata export needed — the page uses the main file's metadata. */}

Short intro — one sentence explaining what the reader gets from this fast path.

## Prerequisites

- List what's needed

## The building block

The core content — commands, config, or resources the reader can grab and go.

```bash
example command
```
